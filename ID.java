 


/**
 * Enumeration class ID - write a description of the enum class here
 * 
 * @author (your name here)
 * @version (version number or date here)
 */
public enum ID      //Enums extend java.lang.Enum. thus, enum cannot extend other classes
{
    Player(), Enemy(), Projectile(), PowerUp();          //Each enum takes in no parameters.
}
